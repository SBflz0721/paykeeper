# KeeperHub — Agents Onchain Hackathon · 项目目标与执行计划

> 一句话定位：**PayKeeper** —— 一个用自然语言定义、通过 KeeperHub 在链上自动执行付款/订阅的 AI Agent。冲「主奖 + Onboarding UX Bounty」双线。

---

## 1. 黑客松硬约束（不可违反）

| 项 | 要求 |
|----|------|
| 唯一强制条件 | 项目**必须**用 KeeperHub 作为链上执行层 |
| 杀手锏交付 | 一条 Agent **真实执行**的链上交易链接（非 mockup） |
| 提交三件套 | GitHub 源码 + 演示视频（展示 Agent 经 KeeperHub 执行）+ 交易链接 |
| 评审基调 | **执行权重最高**，能跑的真实交易 > 精致假 demo |
| 截止 | 2026-08-13 12:00 (UTC+2)；当前约剩 10 天 |

---

## 2. 产品定义：PayKeeper

一个"会付钱的 Agent"。用户用自然语言描述付款意图，Agent 负责**推理 -> 规划 -> 经 KeeperHub 在链上执行 -> 失败重试 -> 出审计**，把"该付的钱按时付掉"。

**为什么选这个方向**：KeeperHub 最独特也最被低估的能力是 **x402/MPP 按次付费** 和 **Gas sponsorship**。支付/订阅场景能一把覆盖最多能力面，且真实可用性最强，并天然贴合 Bounty（做一个"从零到首次 KeeperHub 交易"的模板）。

### 核心用户场景（MVP 闭环）
1. **循环订阅**：用户说"每月 1 号给 `dataapi.xyz` 付 5 USDC" -> Agent 用 KeeperHub `execute_transfer` 在链上定时转账，带审计轨迹、可重试。
2. **按次付费（x402/MPP）**：用户说"需要实时汇率时调用付费汇率工作流" -> Agent 的 agentic wallet 自动拦截 402、签名支付、重试调用，对 Agent 透明。
3. **可观测**：每次动作都回传 trigger / 模拟结果 / 交易哈希 / Gas / 结果 / 时间戳，Agent 汇总成报告。

---

## 3. 技术架构

```
┌─────────────────────────────────────────────┐
│ Agent 层（LangChain / Python，我们写的） │
│ · LLM 推理用户意图 · 规划付款/触发 │
│ · 调用 KeeperHub MCP 工具 · 失败重试 · 报告 │
└───────────────┬─────────────────────────────┘
 │ MCP (HTTP, Bearer kh_*) / Agentic Wallet
┌───────────────▼─────────────────────────────┐
│ KeeperHub 执行层（开源，我们使用其能力） │
│ · MCP Server ── 工具发现与调用 │
│ · Turnkey Wallet ── 真实发交易（Gas 估算/ │
│ 重试/MEV 私有路由/审计轨迹） │
│ · Agentic Wallet ── x402(Base USDC)/MPP( │
│ Tempo USDC.e) 按次付费，facilitator 代付 gas│
│ · Gas Sponsorship（主网 Ethereum） │
└───────────────┬─────────────────────────────┘
 │
 EVM 链：Ethereum / Base / Arbitrum /
 Polygon / Sepolia（测试网先跑）
```

### 已核实的接入要点
- **MCP 入口**：远程 `https://app.keeperhub.com/mcp`（推荐，无需本地进程）；认证用 OAuth 或组织 API Key（`kh_` 前缀）。
- **关键工具**：`execute_transfer`、`execute_contract_call`、`execute_workflow`、`get_execution`、`create_workflow`、`ai_generate_workflow`、`tools_documentation`；付费工作流经 `search_workflows` + `call_workflow`（遇 402 由 wallet 拦截支付）。
- **x402** = Base USDC（`0x8335...2913`）；**MPP** = Tempo USDC.e（`0x20C0...8b50`）；facilitator 代付 gas，钱包只扣 USDC。
- **测试网**：Sepolia 免费先验证；主网可用 Gas sponsorship 去掉 gas 成本。

---

## 4. KeeperHub 能力覆盖 <-> 评分项 对照

| 评分维度 | 我们的落地做法 |
|----------|----------------|
| **经 KeeperHub 真实执行（最高权重）** | `execute_transfer` 循环付款 + x402 调用付费工作流，均产生真实链上交易 |
| **能力面使用广度** | MCP Server、execute_transfer/contract_call（直接执行）、x402/MPP（agentic wallet）、audit trail、Gas sponsorship 全用上 |
| **可靠性/可观测性** | 模拟预飞 (`simulate:true`) -> 广播；失败指数退避重试；全量审计轨迹回传；Agent 输出执行报告 |
| **原创性/真实可用** | "自然语言驱动的自主付款 Agent"，订阅/按次付费是真实需求 |
| **集成质量/DX** | 干净 LangChain 工具封装 + 模板仓库 + 教程，评审可直接复跑 |

---

## 5. 双线冲奖落地

### 主奖（综合排名前三，$2000/$1200/$800）
- 把 PayKeeper Agent 本身做到：能跑真实交易 + 能力面覆盖广 + 可靠性故事完整 + 集成干净。
- 演示视频展示：用户自然语言 -> Agent 经 KeeperHub 完成一次真实链上付款 + x402 付费调用。

### Onboarding UX Bounty（$1000 分 2 名，可叠加）
交付物（任一即可，多做更稳）：
1. **Starter 模板仓库**：`git clone` 后 15 分钟内跑通"Agent 经 KeeperHub 完成首次交易"的最小可用 LangChain 模板。
2. **教程**：《从零到第一次 KeeperHub 执行的 Agent》。
3. **卡点拆解 (teardown)**：记录我们接入时踩的坑 + 改进建议，向 KeeperHub 仓库提 PR。
> 计划三件全做，最大化命中 Bounty。

---

## 6. 仓库结构与 Bounty 交付物

```
paykeeper/
├── agent/ # LangChain Agent：意图解析 + KeeperHub MCP 工具封装
│ ├── agent.py
│ ├── keeperhub_mcp.py # MCP 客户端 + 工具适配
│ └── payments.py # 订阅/按次付费逻辑 + 重试
├── mcp_config.json # KeeperHub MCP 配置（含 kh_ key 占位）
├── .env.example # API key / org / LLM key 占位
├── examples/ # 一次完整自主付款的运行示例与交易哈希
├── docs/
│ ├── TUTORIAL.md # 从零到首次 KeeperHub 交易
│ └── ONBOARDING_TEARDOWN.md # 接入卡点 + 改进建议（提 PR 用）
├── README.md
└── requirements.txt
```

---

## 7. 里程碑（对齐黑客松时间线，UTC+2）

| 阶段 | 时间窗 | 目标 |
|------|--------|------|
| **M0 跑通执行** | 即日–8/5 | 建仓（KeeperHub 账号+Turnkey 钱包）、接 MCP、Agent 在 **Sepolia** 完成**首次** `execute_transfer` 真实交易 |
| **M1 核心功能** | 8/6–8/9 | 订阅调度逻辑、x402 agentic-wallet 付费调用、失败重试 + 审计轨迹报告 |
| **M2 收尾交付** | 8/10–8/13 12:00 | 录演示视频、出 Bounty 模板+教程+teardown PR、主网执行（gas sponsorship）拿提交用交易链接、提交三件套 |
| **缓冲** | 全程 | 主网执行若资金/时间不够，至少保证一条真实 KeeperHub 执行交易（Sepolia 亦可接受为真实执行） |

---

## 8. 前置条件与风险

- **账户/钱包**：需 KeeperHub 账号（自动配 Turnkey 钱包）、组织 API Key（`kh_`）。
- **资金**：Sepolia 测试网靠 faucet；主网执行需少量 ETH（gas sponsorship 可免）+ USDC；x402 测试需 Base Sepolia USDC。
- **LLM Key**：Agent 推理需要（Claude/GPT 等）。
- **开放问题**：提交所需的"真实交易"是否接受测试网？计划以 Sepolia 保底、主网（gas sponsorship）作优选。
- **x402 付费工作流**：需确认是否有现成 KeeperHub 付费工作流可调用，或自行 `deploy` 一个付费工作流用于演示。

---

## 9. 下一步（待你确认后启动）

1. 确认是否要我**立即开始脚手架**（需要你提供 KeeperHub `kh_` key、LLM key，测试网 faucet 我来处理）。
2. 确认**主网 vs 测试网**交易策略（影响是否需要你准备真实资金）。
3. 确认 LLM 选型（默认 Claude；如你有其他偏好告诉我）。
